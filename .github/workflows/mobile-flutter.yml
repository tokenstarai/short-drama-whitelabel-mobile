name: Mobile Flutter

on:
  workflow_dispatch:
  pull_request:
    paths:
      - "mobile/**"
      - ".github/workflows/mobile-flutter.yml"
  push:
    branches:
      - main
    paths:
      - "mobile/**"
      - ".github/workflows/mobile-flutter.yml"

jobs:
  test:
    runs-on: macos-15
    defaults:
      run:
        working-directory: mobile
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - run: ./scripts/check_mobile.sh
      - uses: actions/upload-artifact@v4
        with:
          name: mobile-template-handoff
          path: |
            mobile/build/open-source/short-drama-whitelabel-mobile.zip
            mobile/build/open-source/open-source-template-manifest.json
            mobile/build/store-assets/mobile-store-assets.zip
            mobile/build/store-assets/store-assets-manifest.json
            mobile/build/ios-ci-handoff/mobile-ios-ci-handoff.zip
            mobile/build/ios-ci-handoff/ios-ci-handoff-manifest.json
            mobile/build/store-signing-handoff/mobile-store-signing-handoff.zip
            mobile/build/store-signing-handoff/store-signing-handoff-manifest.json
            mobile/build/store-publish-config/mobile-store-publish-config.zip
            mobile/build/store-publish-config/store-publish-config-manifest.json
            mobile/build/store-submission-evidence/store-submission-evidence.json
            mobile/build/store-submission-evidence/store-submission-evidence.template.json
            mobile/build/store-submission-evidence/store-submission-evidence.guide.md

  android-build:
    runs-on: macos-15
    strategy:
      fail-fast: false
      matrix:
        flavor: [hongguo, douyin, hippo, reelshort]
    defaults:
      run:
        working-directory: mobile
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
      - uses: android-actions/setup-android@v3
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - run: yes | sdkmanager --licenses
      - run: sdkmanager "platforms;android-36" "build-tools;36.0.0" "ndk;28.2.13676358"
      - run: flutter pub get
      - run: ./scripts/build_flavor.sh "${{ matrix.flavor }}" android debug apk
      - run: ./scripts/build_flavor.sh "${{ matrix.flavor }}" android release apk
      - run: ./scripts/build_flavor.sh "${{ matrix.flavor }}" android release appbundle
      - run: ./scripts/write_release_manifest.sh
      - run: ./scripts/write_store_handoff_manifest.py
      - run: ./scripts/scan_release_artifacts.py
      - uses: actions/upload-artifact@v4
        with:
          name: mobile-${{ matrix.flavor }}-android
          path: |
            mobile/build/app/outputs/flutter-apk/app-${{ matrix.flavor }}-debug.apk
            mobile/build/app/outputs/flutter-apk/app-${{ matrix.flavor }}-release.apk
            mobile/build/app/outputs/bundle/**/app-${{ matrix.flavor }}-release.aab
            mobile/build/release-manifests/mobile-artifacts.json
            mobile/build/release-handoff/mobile-store-handoff.json

  ios-build:
    runs-on: macos-15
    strategy:
      fail-fast: false
      matrix:
        flavor: [hongguo, douyin, hippo, reelshort]
    defaults:
      run:
        working-directory: mobile
    steps:
      - uses: actions/checkout@v4
      - uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - run: xcodebuild -version
      - run: pod --version
      - run: flutter pub get
      - run: ./scripts/check_native_config.sh
      - run: ./scripts/build_flavor.sh "${{ matrix.flavor }}" ios debug
      - run: ./scripts/build_flavor.sh "${{ matrix.flavor }}" ios release
      - uses: actions/upload-artifact@v4
        with:
          name: mobile-${{ matrix.flavor }}-ios-unsigned
          path: mobile/build/ios/iphoneos/Runner.app

  ios-ci-evidence:
    runs-on: macos-15
    needs: ios-build
    defaults:
      run:
        working-directory: mobile
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          pattern: mobile-*-ios-unsigned
          path: mobile/build/ci-ios
          merge-multiple: false
      - run: python3 scripts/import_ios_ci_artifacts.py --strict
      - uses: actions/upload-artifact@v4
        with:
          name: mobile-ios-ci-artifact-evidence
          path: |
            mobile/build/ios-ci-evidence/ios-ci-artifacts.json
            mobile/build/ios-ci-evidence/app-info/*.json
